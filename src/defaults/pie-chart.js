import {labelFont} from "./elements/font";

export const defaultPieChartOptions = {
    axis: false,
    other: {
        color: '#eaeaea'
    },
    labels: {
        font: labelFont,
        color: '#fff'
    },
    holeSize: 0,
    legend: {
        vertical: true,
    }
}