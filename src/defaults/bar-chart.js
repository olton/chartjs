import {defaultAxis} from "./elements/axis";

export const defaultBarChartOptions = {
    groupDistance: 20,
    barDistance: 10,
    axis: defaultAxis,
    legend: {
        titles: null
    },

    onDrawLabel: null,
}